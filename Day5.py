# Checking if A list is Empty

my_list = list()

# Check if list is empty by its length
if len(my_list) == 0:
    pass

# Checking by direct comparison(only works for lists)
if my_list == []:
    pass

# Checking by its type felexibility
if not my_list:
    pass
